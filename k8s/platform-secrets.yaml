apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: dev
type: Opaque
stringData:
  # Redis (if authentication needed)
  REDIS_PASSWORD: ""
  
  # RabbitMQ
  RABBITMQ_USER: "admin"
  RABBITMQ_PASS: "rabbitmq123"
  
  # Elasticsearch (if security enabled)
  ELASTICSEARCH_USER: "elastic"
  ELASTICSEARCH_PASS: "elastic123"

